---
phase: 07-review-action-affordances
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - whilly/api/templates/index.html.j2
  - whilly/cli/tui.py
  - tests/integration/test_htmx_dashboard.py
  - tests/unit/test_tui.py
  - docs/superpowers/reviews/2026-05-08-whilly-operator-ui-review.md
autonomous: true
requirements:
  - OPUI-08
must_haves:
  truths:
    - "WUI review controls visibly communicate A Approve, X Reject, and C Changes while keeping decision literals approved, rejected, and changes_requested."
    - "WUI reject and request-changes actions require non-empty trimmed prompt input; cancel or blank input updates operator feedback and returns before fetch."
    - "WUI review clicks and a/x/c hotkeys still post to /api/v1/tasks/{task_id}/human-review through the existing data-review-decision dispatch path."
    - "TUI review controls spell out a=Approve review, x=Reject review, and c=Changes while preserving single-key expert hotkeys."
    - "TUI review decisions still flow through _record_human_review_decision and the shared record_review_decision service path."
    - "The operator UI review audit no longer lists review action affordances as an unresolved finding, except for explicitly narrowed residual risks."
  artifacts:
    - path: "whilly/api/templates/index.html.j2"
      provides: "WUI review action labels, titles/tooltips, prompt/recovery copy, hotkey dispatch, and unchanged review endpoint"
      contains:
        - "A Approve"
        - "Reject review: enter required comment"
        - "Request changes: describe required changes"
        - "/api/v1/tasks/${encodeURIComponent(taskId)}/human-review"
    - path: "tests/integration/test_htmx_dashboard.py"
      provides: "WUI rendered/static contract tests for labels, titles, aria labels, prompt recovery, endpoint, data attributes, and hotkey dispatch"
      contains:
        - "test_dashboard_review_actions_use_clear_affordance_contract"
        - "test_dashboard_review_prompt_recovery_copy_and_hotkey_contract"
    - path: "whilly/cli/tui.py"
      provides: "Clearer TUI review action/help copy while preserving key handling and shared service wiring"
      contains:
        - "a=Approve review"
        - "x=Reject review"
        - "c=Changes"
    - path: "tests/unit/test_tui.py"
      provides: "TUI tests for rendered action/help copy, unchanged key handling, selected-row feedback, and shared service path"
      contains:
        - "test_render_tui_compliance_shows_clear_review_action_help"
        - "test_apply_pending_review_action_requires_selected_actionable_gap"
        - "test_record_human_review_decision_uses_shared_service"
    - path: "docs/superpowers/reviews/2026-05-08-whilly-operator-ui-review.md"
      provides: "Audit trail update resolving or narrowing the stale OPUI-08 remaining finding"
      contains:
        - "Review action affordances"
  key_links:
    - from: "whilly/api/templates/index.html.j2"
      to: "/api/v1/tasks/{task_id}/human-review"
      via: "submitReviewDecision fetch with encodeURIComponent(taskId)"
      pattern: "/api/v1/tasks/\\$\\{encodeURIComponent\\(taskId\\)\\}/human-review"
    - from: "whilly/api/templates/index.html.j2"
      to: "submitReviewDecision(row, decision)"
      via: "button click reads data-review-decision from the clicked review action"
      pattern: "actionButton\\.dataset\\.reviewDecision"
    - from: "whilly/api/templates/index.html.j2"
      to: "submitReviewDecision(selectedReviewRow(), decision)"
      via: "Compliance-surface a/x/c keyboard dispatch"
      pattern: "submitReviewDecision\\(selectedReviewRow\\(\\), \"(approved|rejected|changes_requested)\"\\)"
    - from: "whilly/cli/tui.py"
      to: "record_review_decision"
      via: "_record_human_review_decision builds HumanReviewDecisionCommand and calls the shared service"
      pattern: "record_review_decision\\("
    - from: "tests/integration/test_htmx_dashboard.py"
      to: "whilly/api/templates/index.html.j2"
      via: "rendered dashboard and inline JavaScript contract assertions"
      pattern: "A Approve|Reject canceled|changes_requested"
    - from: "tests/unit/test_tui.py"
      to: "whilly/cli/tui.py"
      via: "Rich render text and state-transition assertions"
      pattern: "a=Approve review|changes_requested"
---

<objective>
Make review actions clearer and safer in both operator interfaces while preserving the expert `a/x/c`
workflow, the existing review endpoint, decision literals, and the shared review-decision service.

Purpose: Phase 7 closes OPUI-08 by improving labels, titles/tooltips, prompt recovery copy, and
rendered TUI help without changing backend review semantics.
Output: Updated WUI template, TUI copy, focused tests, and the operator UI review audit artifact.
</objective>

<execution_context>
@/Users/m.v.shchegolev/.codex/get-shit-done/workflows/execute-plan.md
@/Users/m.v.shchegolev/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-review-action-affordances/07-CONTEXT.md
@.planning/phases/07-review-action-affordances/07-UI-SPEC.md
@.planning/phases/06-mobile-wui-row-actions/06-VERIFICATION.md
@.planning/phases/02-shared-review-decision-path/02-01-SUMMARY.md
@.planning/phases/06-mobile-wui-row-actions/06-01-SUMMARY.md
@docs/superpowers/reviews/2026-05-08-whilly-operator-ui-review.md
@whilly/api/templates/index.html.j2
@whilly/cli/tui.py
@tests/integration/test_htmx_dashboard.py
@tests/unit/test_tui.py

<interfaces>
Locked Phase 7 decisions:

```text
WUI visible action labels: A Approve, X Reject, C Changes
WUI titles: Approve review (a), Reject review - comment required (x), Request changes - details required (c)
WUI aria labels: Approve review for {task_id}, Reject review for {task_id}, Request changes for {task_id}
Decision literals: approved, rejected, changes_requested
Endpoint: POST /api/v1/tasks/{task_id}/human-review
TUI hotkeys: a approves, x rejects, c requests changes
TUI requested-changes note: Requested from TUI operator controls.
```

Existing WUI dispatch contracts to preserve:

```javascript
submitReviewDecision(row, actionButton.dataset.reviewDecision);
submitReviewDecision(selectedReviewRow(), "approved");
submitReviewDecision(selectedReviewRow(), "rejected");
submitReviewDecision(selectedReviewRow(), "changes_requested");
fetch(`/api/v1/tasks/${encodeURIComponent(taskId)}/human-review`, { method: "POST", ... });
```

Existing TUI shared service path to preserve:

```python
handle_tui_key(state, "a") -> state.pending_review_action = "approved"
handle_tui_key(state, "x") -> state.pending_review_action = "rejected"
handle_tui_key(state, "c") -> state.pending_review_action = "changes_requested"
_apply_pending_review_action(...) -> _record_human_review_decision(...)
_record_human_review_decision(...) -> record_review_decision(TaskRepository(pool), HumanReviewDecisionCommand(...))
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pin and implement WUI review action affordances</name>
  <files>whilly/api/templates/index.html.j2, tests/integration/test_htmx_dashboard.py</files>
  <behavior>
    - Test 1: Rendered actionable review row contains visible button labels `A Approve`, `X Reject`, and `C Changes` in that order.
    - Test 2: The same row contains exact titles `Approve review (a)`, `Reject review - comment required (x)`, and `Request changes - details required (c)`.
    - Test 3: Existing aria labels and `data-review-decision` values remain exact: `approved`, `rejected`, `changes_requested`.
    - Test 4: The template keeps `POST /api/v1/tasks/${encodeURIComponent(taskId)}/human-review` and button click dispatch through `actionButton.dataset.reviewDecision`.
    - Test 5: Reject prompt uses `Reject review: enter required comment`; cancel shows `Reject canceled`; blank shows `Reject requires a comment`; both return before fetch.
    - Test 6: Changes prompt uses `Request changes: describe required changes`; cancel shows `Changes canceled`; blank shows `Changes requires details`; both return before fetch.
    - Test 7: Source-control-flow assertions prove reject/changes cancel and blank branches call `setPollingStatus(...)` and `return` before the first `fetch(` in `submitReviewDecision`.
    - Test 8: Desktop `.review-actions button` sizing follows the Phase 7 contract before the media query, while Phase 6 mobile 44px action target rules remain inside the media query.
    - Test 9: Non-actionable review rows still render `read-only` and do not render disabled fake action buttons.
    - Test 10: Approve does not prompt and success statuses name the decision: `Review recorded: approved`, `Review recorded: rejected`, `Review recorded: changes requested`.
    - Test 11: WUI `a/x/c` hotkeys still dispatch only on the Compliance surface to the same decision literals.
  </behavior>
  <action>
    RED:
    - In `tests/integration/test_htmx_dashboard.py`, update the existing review-action coverage from Phase 6 and add two focused tests:
      - `test_dashboard_review_actions_use_clear_affordance_contract`
      - `test_dashboard_review_prompt_recovery_copy_and_hotkey_contract`
    - Use existing helpers `_seed_plan`, `_seed_worker`, `_seed_task`, `_seed_event`, `_table_block`, and `_row_block`.
    - Seed one actionable human-review row with task id `t-human-review` and stage `release_review`.
    - In `test_dashboard_review_actions_use_clear_affordance_contract`, seed one actionable row and one non-actionable/read-only review gap. Extract the actionable review row and assert:
      - `>A Approve</button>`, `>X Reject</button>`, and `>C Changes</button>` are present and ordered Approve, Reject, Changes.
      - `title="Approve review (a)"`, `title="Reject review - comment required (x)"`, and `title="Request changes - details required (c)"` are present.
      - `aria-label="Approve review for t-human-review"`, `aria-label="Reject review for t-human-review"`, and `aria-label="Request changes for t-human-review"` are present.
      - `data-review-decision="approved"`, `data-review-decision="rejected"`, and `data-review-decision="changes_requested"` are present.
      - Extract the non-actionable row and assert `read-only` is present, `data-review-decision=` is absent, and `<button` is absent.
      - The CSS before `@media (max-width: 900px)` contains `.review-actions button`, `min-height: 32px;`, `padding: 4px 8px;`, and `border-radius: 2px;`.
      - The CSS from `@media (max-width: 900px)` onward still contains `#review-gaps .review-actions button`, `min-width: 44px;`, `min-height: 44px;`, and `padding: 8px;`.
    - In `test_dashboard_review_prompt_recovery_copy_and_hotkey_contract`, assert the full page source contains:
      - `No review row selected`, `Admin token required`, and `Reviewer required`.
      - `window.prompt("Reject review: enter required comment", "")`.
      - `Reject canceled` and `Reject requires a comment`.
      - `window.prompt("Request changes: describe required changes", "")`.
      - `Changes canceled` and `Changes requires details`.
      - `Review recorded: approved`, `Review recorded: rejected`, and `Review recorded: changes requested`.
      - ``/api/v1/tasks/${encodeURIComponent(taskId)}/human-review``.
      - `actionButton.dataset.reviewDecision`.
      - `submitReviewDecision(selectedReviewRow(), "approved")`, `submitReviewDecision(selectedReviewRow(), "rejected")`, and `submitReviewDecision(selectedReviewRow(), "changes_requested")`.
    - In the same prompt-recovery test, add a helper that slices the `submitReviewDecision(row, decision)` function body up to the first `fetch(`. Assert that pre-fetch slice contains, in branch order, each status plus a following `return;`:
      - `setPollingStatus("Reject canceled");` followed by `return;`.
      - `setPollingStatus("Reject requires a comment", true);` followed by `return;`.
      - `setPollingStatus("Changes canceled");` followed by `return;`.
      - `setPollingStatus("Changes requires details", true);` followed by `return;`.
      - The pre-fetch slice does not contain `fetch(`.
    - Update existing assertions that currently expect bare `>A<`, `>X<`, `>C<`, or lowercase hotkey help to expect the Phase 7 labels/copy.
    - Run the new tests and confirm they fail before implementation.

    GREEN:
    - In `whilly/api/templates/index.html.j2`, update only the WUI review-action presentation and inline review JavaScript.
    - Change actionable review button visible text to `A Approve`, `X Reject`, and `C Changes` while preserving the current button order.
    - Set titles exactly to `Approve review (a)`, `Reject review - comment required (x)`, and `Request changes - details required (c)`.
    - Preserve existing `aria-label="Approve review for {{ gap.task_id }}"`, `aria-label="Reject review for {{ gap.task_id }}"`, and `aria-label="Request changes for {{ gap.task_id }}"`.
    - Preserve exact `data-review-decision` literals: `approved`, `rejected`, `changes_requested`.
    - Adjust desktop `.review-actions button` sizing to the UI contract: `min-height: 32px; padding: 4px 8px; border-radius: 2px;`. Keep semantic border colors and do not add filled default colors.
    - Keep the Phase 6 mobile rules for `min-width: 44px`, `min-height: 44px`, `padding: 8px`, wrapping actions, and the full-width Actions cell.
    - Update the hotkey legend from lowercase meanings to visible, clearer meanings: `a=Approve review`, `x=Reject review`, `c=Changes`. Keep `j/k=select` and all other hotkeys unchanged.
    - In `submitReviewDecision(row, decision)`:
      - For missing `row`, call `setPollingStatus("No review row selected", true); return;`.
      - Leave missing admin token and missing reviewer behavior/copy as `Admin token required` and `Reviewer required`.
      - Build `payload = { decision, reviewer }` and preserve `stage_id` behavior.
      - For `decision === "rejected"`, prompt exactly `Reject review: enter required comment`. If `comment === null`, call `setPollingStatus("Reject canceled"); return;`. If `!comment.trim()`, call `setPollingStatus("Reject requires a comment", true); return;`. Otherwise set `payload.comment = comment.trim()`.
      - For `decision === "changes_requested"`, prompt exactly `Request changes: describe required changes`. If `requested === null`, call `setPollingStatus("Changes canceled"); return;`. If `!requested.trim()`, call `setPollingStatus("Changes requires details", true); return;`. Otherwise set `payload.requested_changes = [requested.trim()]`.
      - Do not prompt for `approved`.
      - Keep the existing `fetch(`/api/v1/tasks/${encodeURIComponent(taskId)}/human-review`, ...)` endpoint and headers.
      - On successful response, set status to a decision-specific message using a small local mapping: `approved -> Review recorded: approved`, `rejected -> Review recorded: rejected`, `changes_requested -> Review recorded: changes requested`.
      - For non-OK responses, update the status region with `Review not recorded: ${detail}. Check admin token, reviewer, and selected row, then retry.` and return.
      - For network errors, update the status region with `Review not recorded: ${error}. Check admin token, reviewer, and selected row, then retry.`.
    - Do not change backend routes, request decision literals, SSE/HTMX triggers, sessionStorage keys, mobile stacked-row selectors, table ids, or pause/resume controls.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -q tests/integration/test_htmx_dashboard.py::test_dashboard_review_actions_use_clear_affordance_contract tests/integration/test_htmx_dashboard.py::test_dashboard_review_prompt_recovery_copy_and_hotkey_contract tests/integration/test_htmx_dashboard.py::test_dashboard_mobile_labels_match_table_contract_and_review_actions_are_accessible tests/integration/test_htmx_dashboard.py::test_dashboard_mirrors_operator_surfaces_and_hotkeys tests/integration/test_htmx_dashboard.py::test_dashboard_renders_compliance_gaps_and_events</automated>
  </verify>
  <done>WUI review actions are visibly self-explanatory, cancel/blank reject and changes prompts leave tasks unchanged before fetch, and expert click/hotkey dispatch still reaches the unchanged review endpoint with the locked decision literals.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pin and implement TUI review action copy without key-path changes</name>
  <files>whilly/cli/tui.py, tests/unit/test_tui.py</files>
  <behavior>
    - Test 1: Rendered TUI help includes `a=Approve review`, `x=Reject review`, and `c=Changes`.
    - Test 2: The Compliance table action cell remains compact as `a/x/c` for actionable rows and keeps the `>` selection marker.
    - Test 3: `handle_tui_key` still maps `a`, `x`, and `c` to `approved`, `rejected`, and `changes_requested`.
    - Test 4: Missing reviewer copy is exact: `reviewer required: pass --reviewer or set WHILLY_OPERATOR_EMAIL`.
    - Test 5: No selected actionable gap copy is exact: `no actionable human review gap selected`.
    - Test 6: `_record_human_review_decision` still calls the shared `record_review_decision` service with source `tui`, stage id, reviewer, decision, and fixed requested-changes note.
  </behavior>
  <action>
    RED:
    - In `tests/unit/test_tui.py`, update `test_render_tui_overview_includes_surfaces_and_hotkeys` to expect `a=Approve review`, `x=Reject review`, and `c=Changes` instead of lowercase action meanings.
    - Rename or add `test_render_tui_compliance_shows_clear_review_action_help` so it renders `OperatorSurface.COMPLIANCE` and asserts:
      - `Compliance - Human review / verification gaps` is present.
      - The selected marker `>` is present.
      - The compact action cell `a/x/c` is present.
      - The caption/help text contains `a=Approve review`, `x=Reject review`, and `c=Changes`.
    - Keep `test_handle_tui_key_switches_views_filter_pause_refresh_review_actions_and_quit` asserting the exact decision literals for `a`, `x`, and `c`.
    - Strengthen `test_apply_pending_review_action_requires_reviewer` to assert `state.last_error == "reviewer required: pass --reviewer or set WHILLY_OPERATOR_EMAIL"`.
    - Add `test_apply_pending_review_action_requires_selected_actionable_gap`: create a snapshot with no `review_gaps`, set `pending_review_action="rejected"`, call `_apply_pending_review_action(..., reviewer="lead@example.com")`, and assert it returns `False`, clears `pending_review_action`, leaves `immediate_refresh` false, records no service call, and sets `state.last_error == "no actionable human review gap selected"`.
    - Keep or extend `test_record_human_review_decision_uses_shared_service` to assert `command.source == "tui"`, `command.stage_id == "release_review"`, `command.decision == "changes_requested"`, and `command.requested_changes == ("Requested from TUI operator controls.",)`.
    - Run the new/updated TUI tests and confirm the help-copy assertions fail before implementation.

    GREEN:
    - In `whilly/cli/tui.py`, update only rendered help/caption copy and any missing exact error copy needed by the tests.
    - In `_header`, change the review-action help fragment to exactly `j/k=select  a=Approve review  x=Reject review  c=Changes`.
    - Keep the existing `handle_tui_key` mapping for `a/A`, `x/X`, and `c/C`; do not add blocking input, prompts, or multi-step modal state to the TUI.
    - Keep `_compliance_table` action cells as `a/x/c` for actionable rows and `-` for non-actionable rows.
    - Keep `_apply_pending_review_action` clearing `pending_review_action` before validation, requiring reviewer, selecting only actionable filtered gaps, and setting `immediate_refresh` only after a successful record.
    - Preserve `_record_human_review_decision` and its shared service call. Do not import or call any backend route directly from the TUI.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -q tests/unit/test_tui.py::test_render_tui_overview_includes_surfaces_and_hotkeys tests/unit/test_tui.py::test_render_tui_compliance_shows_clear_review_action_help tests/unit/test_tui.py::test_handle_tui_key_switches_views_filter_pause_refresh_review_actions_and_quit tests/unit/test_tui.py::test_apply_pending_review_action_requires_reviewer tests/unit/test_tui.py::test_apply_pending_review_action_requires_selected_actionable_gap tests/unit/test_tui.py::test_record_human_review_decision_uses_shared_service</automated>
  </verify>
  <done>TUI rendered help explains the review actions, the compact action cell and expert hotkeys remain unchanged, and the shared review-decision service path remains the only recording path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Update the operator UI review audit and run final verification</name>
  <files>docs/superpowers/reviews/2026-05-08-whilly-operator-ui-review.md, tests/integration/test_htmx_dashboard.py, tests/unit/test_tui.py</files>
  <behavior>
    - Test 1: The audit doc no longer contains stale Remaining Findings for Phase 5 table contracts, Phase 6 mobile tables, or Phase 7 review action affordances.
    - Test 2: The audit doc no longer recommends the completed Phase 5, Phase 6, or Phase 7 tasks.
    - Test 3: The audit doc records Phase 5, Phase 6, and Phase 7 as resolved, or explicitly narrows remaining risk to non-implemented true undo / live browser or assistive-technology QA.
    - Test 4: Full WUI and TUI test files pass together after the doc update.
    - Test 5: Ruff passes for edited Python files.
  </behavior>
  <action>
    RED:
    - Before editing the audit doc, run the Python doc-check command from this task's verification block and observe it fail because the stale remaining finding is still present.

    GREEN:
    - Update `docs/superpowers/reviews/2026-05-08-whilly-operator-ui-review.md` after Tasks 1 and 2 are implemented.
    - In `Resolved In This Pass`, add concise bullets for:
      - Phase 5 shared table contract if still missing from the resolved list.
      - Phase 6 mobile stacked row/action layout if still missing from the resolved list.
      - Phase 7 review action affordances: WUI labels/titles, prompt cancel/blank recovery, unchanged endpoint/hotkeys, and clearer TUI help.
    - Remove or rewrite the stale Remaining Findings:
      - `Mobile tables are functional but cramped.`
      - `Table contracts are not fully identical.`
      - `Review actions need stronger affordance.`
    - If any risk remains, narrow it to concrete residuals only, such as true undo being deferred because it requires backend/audit reversal semantics, and browser/screen-reader QA not being captured in this environment.
    - Remove the completed recommended next tasks:
      - `Add a mobile row-detail/action layout for the WUI dashboard tables.`
      - `Define a shared TUI/WUI table-column contract for tasks, workers, review queue, and events.`
      - `Add clearer review-action affordances for reject/request-changes paths.`
    - Replace recommendations only with narrowed follow-ups if still accurate, for example live browser/screen-reader QA or a future backend undo/audit-reversal phase.
    - If updating score fields, keep the scoring honest: workflow clarity and responsive resilience may improve, while risk/recovery should not claim full credit for true undo because undo is explicitly deferred.
    - Do not modify roadmap or implementation files in this task beyond fixing test fallout from Tasks 1 and 2.
  </action>
  <verify>
    <automated>.venv/bin/python -c 'from pathlib import Path; text = Path("docs/superpowers/reviews/2026-05-08-whilly-operator-ui-review.md").read_text(); stale = ["Mobile tables are functional but cramped.", "Table contracts are not fully identical.", "Review actions need stronger affordance.", "Add a mobile row-detail/action layout for the WUI dashboard tables.", "Define a shared TUI/WUI table-column contract for tasks, workers, review queue, and events.", "Add clearer review-action affordances for reject/request-changes paths."]; missing = [s for s in stale if s in text]; assert not missing, missing; assert "Review action affordances" in text or "review action affordances" in text; assert "undo" in text.lower() or "screen-reader" in text.lower() or "browser" in text.lower()'</automated>
    <automated>.venv/bin/python -m pytest -q tests/integration/test_htmx_dashboard.py tests/unit/test_tui.py</automated>
    <automated>.venv/bin/python -m ruff check whilly/cli/tui.py tests/integration/test_htmx_dashboard.py tests/unit/test_tui.py</automated>
  </verify>
  <done>The audit artifact reflects the implemented Phase 7 affordance work, stale remaining/recommended entries are removed or narrowed, and the complete edited WUI/TUI test surface plus Ruff pass.</done>
</task>

</tasks>

<verification>
Run these commands before declaring the phase complete:

```bash
.venv/bin/python -m pytest -q tests/integration/test_htmx_dashboard.py::test_dashboard_review_actions_use_clear_affordance_contract tests/integration/test_htmx_dashboard.py::test_dashboard_review_prompt_recovery_copy_and_hotkey_contract tests/integration/test_htmx_dashboard.py::test_dashboard_mobile_labels_match_table_contract_and_review_actions_are_accessible
.venv/bin/python -m pytest -q tests/unit/test_tui.py::test_render_tui_overview_includes_surfaces_and_hotkeys tests/unit/test_tui.py::test_render_tui_compliance_shows_clear_review_action_help tests/unit/test_tui.py::test_handle_tui_key_switches_views_filter_pause_refresh_review_actions_and_quit tests/unit/test_tui.py::test_apply_pending_review_action_requires_reviewer tests/unit/test_tui.py::test_apply_pending_review_action_requires_selected_actionable_gap tests/unit/test_tui.py::test_record_human_review_decision_uses_shared_service
.venv/bin/python -m pytest -q tests/integration/test_htmx_dashboard.py tests/unit/test_tui.py
.venv/bin/python -m ruff check whilly/cli/tui.py tests/integration/test_htmx_dashboard.py tests/unit/test_tui.py
.venv/bin/python -c 'from pathlib import Path; text = Path("docs/superpowers/reviews/2026-05-08-whilly-operator-ui-review.md").read_text(); assert "Review actions need stronger affordance." not in text; assert "Add clearer review-action affordances for reject/request-changes paths." not in text'
```

If Docker-backed dashboard integration tests are skipped because Docker is unavailable, state that
explicitly in the summary and still run `tests/unit/test_tui.py`, Ruff, and the audit-doc check.
</verification>

<success_criteria>
- OPUI-08 is satisfied with UI-only changes plus tests and audit-doc cleanup.
- WUI buttons visibly read `A Approve`, `X Reject`, and `C Changes`, with exact titles, aria labels, and preserved decision data attributes.
- WUI reject/request-changes cancel and blank prompt paths update the status region and return before `fetch`.
- WUI endpoint path, payload decision literals, click dispatch, and Compliance-surface hotkey dispatch remain unchanged.
- TUI rendered help spells out review actions while `a/x/c` key handling and shared service recording remain unchanged.
- `docs/superpowers/reviews/2026-05-08-whilly-operator-ui-review.md` no longer leaves OPUI-08 as a stale unresolved finding.
</success_criteria>

<output>
After completion, create `.planning/phases/07-review-action-affordances/07-01-SUMMARY.md` with tests run, files modified, confirmation that no backend route/service semantics changed, and any skipped Docker-backed checks.
</output>
