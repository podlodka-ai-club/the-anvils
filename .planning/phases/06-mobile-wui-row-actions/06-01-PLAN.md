---
phase: 06-mobile-wui-row-actions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - whilly/api/templates/index.html.j2
  - whilly/api/templates/_tasks_table.html
  - whilly/api/templates/_workers_table.html
  - tests/integration/test_htmx_dashboard.py
autonomous: true
requirements:
  - OPUI-06
must_haves:
  truths:
    - "Mobile compliance review rows show Task, Plan, Reason, Stage, Reviewer, and Actions as stacked touch-friendly fields."
    - "Mobile task, worker, and event rows are scannable below 900px without critical text/action overlap."
    - "Review action buttons remain A/X/C visually, have per-task aria labels, and meet 44px touch target sizing below 900px."
    - "Desktop table headers, table ids, row ids, HTMX attributes, data-filter-text, and table-column labels remain unchanged above 900px."
    - "Task and worker fragments include the same mobile-label contract as full-page rendering."
  artifacts:
    - path: "whilly/api/templates/index.html.j2"
      provides: "Review/event mobile labels, review action aria labels, and max-width 900px stacked table CSS"
      contains: ["@media (max-width: 900px)", "data-mobile-label", "aria-label=\"Approve review for"]
    - path: "whilly/api/templates/_tasks_table.html"
      provides: "Task row mobile labels in the polling fragment"
      contains: ["data-mobile-label", "task_columns"]
    - path: "whilly/api/templates/_workers_table.html"
      provides: "Worker row mobile labels in the polling fragment"
      contains: ["data-mobile-label", "worker_columns"]
    - path: "tests/integration/test_htmx_dashboard.py"
      provides: "Static/integration assertions for mobile label, accessibility, CSS, fragment, and desktop-preservation contracts"
      contains: ["test_dashboard_mobile_labels_match_table_contract_and_review_actions_are_accessible", "test_dashboard_mobile_table_css_contract"]
  key_links:
    - from: "whilly/operator_views.py"
      to: "whilly/api/templates/index.html.j2"
      via: "table_columns.review_gaps/events column.label_for('wui') values used in data-mobile-label attributes"
      pattern: "data-mobile-label=\"{{ .*\\.label_for\\(['\\\"]wui['\\\"]\\) }}\""
    - from: "whilly/operator_views.py"
      to: "whilly/api/templates/_tasks_table.html"
      via: "table_columns.tasks column.label_for('wui') values used in data-mobile-label attributes"
      pattern: "task_columns\\[[0-9]+\\]\\.label_for\\(['\\\"]wui['\\\"]\\)"
    - from: "whilly/operator_views.py"
      to: "whilly/api/templates/_workers_table.html"
      via: "table_columns.workers column.label_for('wui') values used in data-mobile-label attributes"
      pattern: "worker_columns\\[[0-9]+\\]\\.label_for\\(['\\\"]wui['\\\"]\\)"
    - from: "tests/integration/test_htmx_dashboard.py"
      to: "whilly/api/templates/index.html.j2"
      via: "rendered HTML assertions compare data-mobile-label values with table headers and assert mobile CSS selectors"
      pattern: "data-mobile-label|@media \\(max-width: 900px\\)"
---

<objective>
Make the WUI operator tables usable on mobile by adding metadata-derived per-cell mobile labels,
mobile-only stacked row CSS, and touch-friendly review action controls while preserving desktop table behavior.

Purpose: Phase 6 closes OPUI-06 by making critical row details and review actions usable below 900px without cramped horizontal scrolling.
Output: Updated Jinja templates and integration tests. No backend API, TUI, pause/resume, or review-decision semantics changes.
</objective>

<execution_context>
@/Users/m.v.shchegolev/.codex/get-shit-done/workflows/execute-plan.md
@/Users/m.v.shchegolev/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-mobile-wui-row-actions/06-CONTEXT.md
@.planning/phases/06-mobile-wui-row-actions/06-UI-SPEC.md
@.planning/phases/05-shared-operator-table-contract/05-01-SUMMARY.md
@whilly/operator_views.py
@whilly/api/templates/index.html.j2
@whilly/api/templates/_tasks_table.html
@whilly/api/templates/_workers_table.html
@tests/integration/test_htmx_dashboard.py

<interfaces>
Phase 5 created the shared table contract in `whilly/operator_views.py`.

```python
class OperatorTable(str, Enum):
    TASKS = "tasks"
    WORKERS = "workers"
    REVIEW_GAPS = "review_gaps"
    EVENTS = "events"

@dataclass(frozen=True)
class OperatorTableColumn:
    field_key: str
    canonical_label: str
    tui_label: str | None = None
    wui_label: str | None = None
    show_tui: bool = True
    show_wui: bool = True
    medium_note: str = ""

    def label_for(self, medium: OperatorMedium) -> str: ...

def operator_table_columns(table: OperatorTable | str, medium: OperatorMedium) -> tuple[OperatorTableColumn, ...]: ...
def operator_table_labels(table: OperatorTable | str, medium: OperatorMedium) -> tuple[str, ...]: ...
```

The WUI context already exposes `table_columns.tasks`, `table_columns.workers`,
`table_columns.review_gaps`, and `table_columns.events`. Use those objects directly for mobile labels.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add metadata-derived mobile labels and accessible review action names</name>
  <files>whilly/api/templates/index.html.j2, whilly/api/templates/_tasks_table.html, whilly/api/templates/_workers_table.html, tests/integration/test_htmx_dashboard.py</files>
  <read_first>
    - .planning/phases/06-mobile-wui-row-actions/06-UI-SPEC.md
    - whilly/operator_views.py
    - whilly/api/templates/index.html.j2
    - whilly/api/templates/_tasks_table.html
    - whilly/api/templates/_workers_table.html
    - tests/integration/test_htmx_dashboard.py
  </read_first>
  <behavior>
    - Rendered non-empty task rows expose mobile labels exactly matching their WUI header labels: Task, Plan, Status, Priority, Worker, Review, Updated.
    - Rendered non-empty worker rows expose mobile labels exactly matching their WUI header labels: Worker, Hostname, Owner, Status, Last heartbeat.
    - Rendered non-empty review rows expose mobile labels exactly matching their WUI header labels: Task, Plan, Reason, Stage, Reviewer, Actions.
    - Rendered non-empty event rows expose mobile labels exactly matching their WUI header labels: Id, Task, Plan, Type, At.
    - Task and worker HTMX fragments include the same `data-mobile-label` attributes as the full-page render.
    - Review action buttons keep visible labels `A`, `X`, and `C`, but add `aria-label="Approve review for {task_id}"`, `aria-label="Reject review for {task_id}"`, and `aria-label="Request changes for {task_id}"`.
  </behavior>
  <action>
    First update `tests/integration/test_htmx_dashboard.py`:
    - Add helper `def _mobile_labels(row_html: str) -> list[str]:` returning `re.findall(r'data-mobile-label="([^"]+)"', row_html)`.
    - Add `test_dashboard_mobile_labels_match_table_contract_and_review_actions_are_accessible`.
    - In the new test, seed one plan, worker, task, review-required event, and normal event using existing helper functions.
    - Render `/`, extract `tasks`, `workers`, `review-gaps`, and `events` with `_table_block`, extract one non-empty row with `_row_block`, and assert `_mobile_labels(row) == _header_labels(table)` for each table.
    - In the review row, assert exact strings for `aria-label="Approve review for t-human-review"`, `aria-label="Reject review for t-human-review"`, and `aria-label="Request changes for t-human-review"`.
    - Assert the review row still contains `data-review-decision="approved"`, `data-review-decision="rejected"`, `data-review-decision="changes_requested"`, and visible `>A<`, `>X<`, `>C<` button text.
    - Extend `test_workers_fragment_returns_just_workers_table` and `test_tasks_fragment_returns_just_tasks_table` to assert the fragment row mobile labels equal fragment header labels.

    Then update templates:
    - In `_tasks_table.html`, set `{% set task_columns = table_columns.tasks %}` near the top and keep the existing header loop label output unchanged. Add `data-mobile-label="{{ task_columns[0].label_for("wui") }}"` through `data-mobile-label="{{ task_columns[6].label_for("wui") }}"` to the seven populated-row `<td>` cells in current column order. Do not add mobile labels to `#tasks-empty`.
    - In `_workers_table.html`, set `{% set worker_columns = table_columns.workers %}` near the top and keep the existing header loop label output unchanged. Add `data-mobile-label="{{ worker_columns[0].label_for("wui") }}"` through `data-mobile-label="{{ worker_columns[4].label_for("wui") }}"` to the five populated-row `<td>` cells in current column order. Do not add mobile labels to `#workers-empty`.
    - In `index.html.j2` review table, set `{% set review_columns = table_columns.review_gaps %}` before rendering the review table. Add `data-mobile-label="{{ review_columns[0].label_for("wui") }}"` through `data-mobile-label="{{ review_columns[5].label_for("wui") }}"` to the six populated-row `<td>` cells in current column order. Do not add mobile labels to `#review-gaps-empty`.
    - In `index.html.j2` event table, set `{% set event_columns = table_columns.events %}` before rendering the event table. Add `data-mobile-label="{{ event_columns[0].label_for("wui") }}"` through `data-mobile-label="{{ event_columns[4].label_for("wui") }}"` to the five populated-row `<td>` cells in current column order. Do not add mobile labels to `#events-empty`.
    - Add the exact review button accessible names on the existing buttons: `aria-label="Approve review for {{ gap.task_id }}"`, `aria-label="Reject review for {{ gap.task_id }}"`, and `aria-label="Request changes for {{ gap.task_id }}"`.
    - Preserve existing table ids, row ids, `<thead>`, `role="grid"`, `hx-*`, `sse:*`, `data-filter-text`, `data-review-*`, `aria-selected`, titles, visible button labels, and empty-state copy.
  </action>
  <acceptance_criteria>
    - Every non-empty WUI task, worker, review, and event row has one `data-mobile-label` per visible table header.
    - Mobile labels are sourced from `table_columns.*[index].label_for("wui")`, not duplicated hard-coded label strings.
    - Task and worker fragments render mobile labels.
    - Review action visible labels and decision data attributes are unchanged, and each action has a per-task `aria-label`.
    - Empty-state rows keep their existing copy and do not require label/value mobile presentation.
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest -q tests/integration/test_htmx_dashboard.py::test_dashboard_mobile_labels_match_table_contract_and_review_actions_are_accessible tests/integration/test_htmx_dashboard.py::test_workers_fragment_returns_just_workers_table tests/integration/test_htmx_dashboard.py::test_tasks_fragment_returns_just_tasks_table</automated>
  </verify>
  <done>Rendered HTML proves Phase 5 WUI table labels are preserved as mobile row labels, review actions are accessible by task, and polling fragments carry the same mobile contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add mobile-only stacked row CSS and touch target contract</name>
  <files>whilly/api/templates/index.html.j2, tests/integration/test_htmx_dashboard.py</files>
  <read_first>
    - .planning/phases/06-mobile-wui-row-actions/06-UI-SPEC.md
    - whilly/api/templates/index.html.j2
    - tests/integration/test_htmx_dashboard.py
  </read_first>
  <behavior>
    - At `max-width: 900px`, review, task, worker, and event body rows render as full-width stacked rows with labeled fields and wrapping values.
    - At `max-width: 900px`, review Actions render as a full-width row section after details, with buttons in a wrapping flex row.
    - At `max-width: 900px`, review action buttons have `min-width: 44px`, `min-height: 44px`, `padding: 8px`, and an `8px` gap between buttons.
    - At `max-width: 900px`, long ids, hostnames, event types, timestamps, reasons, and reviewer values wrap with `overflow-wrap: anywhere`.
    - Above 900px, existing desktop table headers, nowrap behavior, and `.table-wrapper { overflow-x: auto; }` fallback remain unchanged except for harmless `data-mobile-label`/`aria-label` attributes.
  </behavior>
  <action>
    First update `tests/integration/test_htmx_dashboard.py`:
    - Add `test_dashboard_mobile_table_css_contract`.
    - Render `/`, slice the response text from `@media (max-width: 900px)` onward into `mobile_css`, and assert it contains each exact selector/property contract listed below.
    - Assert the full body still contains `.table-wrapper { overflow-x: auto; }` before the media query.
    - Assert `mobile_css` contains `#review-gaps tbody tr[data-filter-text]`, `#tasks tbody tr[data-filter-text]`, `#workers tbody tr[data-filter-text]`, and `#events tbody tr[data-filter-text]`.
    - Assert `mobile_css` contains `grid-template-columns: minmax(84px, 32%) minmax(0, 1fr);`, `content: attr(data-mobile-label);`, `white-space: normal;`, `overflow-wrap: anywhere;`, `border-radius: 4px;`, `gap: 8px;`, `min-width: 44px;`, `min-height: 44px;`, and `padding: 8px;`.
    - Assert `mobile_css` contains a hidden-row override for filtered rows: `tr[data-filter-text][hidden]`.
    - Assert `mobile_css` contains `#review-gaps td[data-mobile-label="Actions"]` and `#review-gaps .review-actions button`.

    Then update only the existing `@media (max-width: 900px)` block in `index.html.j2`; do not move desktop CSS outside the media query.
    Add these mobile-only CSS contracts:
    - Scope row stacking to `#review-gaps`, `#tasks`, `#workers`, and `#events` only.
    - Keep the current mobile shell, metric-strip, topbar, tab, and general table font rules.
    - Set `#surface-compliance .table-wrapper`, `#surface-plans_tasks .table-wrapper`, `#surface-workers .table-wrapper`, and `#surface-events .table-wrapper` to `overflow-x: visible;`.
    - Set `#review-gaps`, `#tasks`, `#workers`, and `#events` to `display: block; width: 100%; border: 0; background: transparent;`.
    - Visually hide the four affected `<thead>` elements inside the media query with `position: absolute; width: 1px; height: 1px; margin: -1px; padding: 0; overflow: hidden; clip: rect(0 0 0 0); white-space: nowrap; border: 0;`.
    - Set the four affected `tbody` elements to `display: grid; gap: 8px;`.
    - Set `#review-gaps tbody tr[data-filter-text]`, `#tasks tbody tr[data-filter-text]`, `#workers tbody tr[data-filter-text]`, and `#events tbody tr[data-filter-text]` to `display: grid; grid-template-columns: 1fr; gap: 4px; width: 100%; margin: 0 0 8px; border: 1px solid var(--line); border-radius: 4px; background: var(--surface); overflow: hidden;`.
    - Add explicit `tr[data-filter-text][hidden] { display: none; }` coverage for those four tables so the existing filter still hides rows after author-level mobile display rules.
    - Set affected `td[data-mobile-label]` cells to `display: grid; grid-template-columns: minmax(84px, 32%) minmax(0, 1fr); gap: 8px; align-items: start; min-width: 0; padding: 4px 8px; border-bottom: 0; white-space: normal; overflow-wrap: anywhere;`.
    - Add affected `td[data-mobile-label]::before` labels with `content: attr(data-mobile-label); color: var(--muted); font-size: 12px; font-weight: 600; line-height: 1.2;`.
    - Set `#review-gaps td[data-mobile-label="Actions"]` to `display: block; padding: 8px; border-top: 1px solid var(--line);`.
    - Set `#review-gaps td[data-mobile-label="Actions"]::before` to `display: block; margin-bottom: 4px;`.
    - Set `#review-gaps .review-actions` to `display: flex; flex-wrap: wrap; gap: 8px; align-items: center; white-space: normal;`.
    - Set `#review-gaps .review-actions button` to `min-width: 44px; min-height: 44px; padding: 8px;`.
    - Set code and time values inside the four tables to wrap with `overflow-wrap: anywhere;`.
    - Set `#tasks td[data-mobile-label="Review"] code` to `display: block; margin-top: 4px;` so review stage can wrap below the decision.
    - Add mobile empty-row rules for `#review-gaps-empty`, `#tasks-empty`, `#workers-empty`, and `#events-empty`: row `display: block; border: 1px solid var(--line); border-radius: 4px; background: var(--surface);` and child `td` `display: block; padding: 8px; white-space: normal; border-bottom: 0;`.
    - Do not introduce new colors beyond existing CSS variables, new animations, new cards nested inside rows, new JavaScript, new server endpoints, or any visible review-action copy changes.
  </action>
  <acceptance_criteria>
    - All stacked-row CSS is inside `@media (max-width: 900px)` and scoped to review, task, worker, and event tables.
    - Critical review actions are full-width below row details and cannot shrink below 44px touch targets.
    - Text values wrap on mobile using `white-space: normal` and `overflow-wrap: anywhere`; filtered hidden rows remain hidden.
    - Desktop `.table-wrapper { overflow-x: auto; }`, headers, table order, row ids, and behavior remain intact above 900px.
    - The CSS contract test asserts the exact selectors and sizing properties needed by OPUI-06.
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest -q tests/integration/test_htmx_dashboard.py::test_dashboard_mobile_table_css_contract tests/integration/test_htmx_dashboard.py::test_dashboard_mobile_responsive_block tests/integration/test_htmx_dashboard.py::test_dashboard_table_contract_headers_and_worker_order tests/integration/test_htmx_dashboard.py::test_dashboard_preserves_local_state_across_refresh_and_sse_swaps</automated>
  </verify>
  <done>Mobile CSS stacks the four operator tables below 900px, preserves filtering/refresh/desktop contracts, and pins touch target behavior with integration assertions.</done>
</task>

</tasks>

<verification>
Run focused verification first, then the full dashboard integration file:

```bash
.venv/bin/python -m pytest -q tests/integration/test_htmx_dashboard.py::test_dashboard_mobile_labels_match_table_contract_and_review_actions_are_accessible tests/integration/test_htmx_dashboard.py::test_dashboard_mobile_table_css_contract
.venv/bin/python -m pytest -q tests/integration/test_htmx_dashboard.py
.venv/bin/python -m ruff check tests/integration/test_htmx_dashboard.py
```

If Docker-backed integration tests are skipped because Docker is unavailable, report that explicitly in the summary and still run Ruff on the edited Python test file.
</verification>

<success_criteria>
- OPUI-06 is implemented without backend, TUI, pause/resume, or review-decision semantic changes.
- Mobile review rows expose details and actions in a touch-friendly stacked layout below 900px.
- Mobile task, worker, and event rows expose metadata-derived labels and wrap long values without overlapping controls.
- Review buttons remain visually compact as `A`, `X`, and `C`, add exact task-specific aria labels, and meet 44px mobile touch targets.
- Existing desktop table behavior and Phase 5 table-contract labels are preserved.
</success_criteria>

<output>
After completion, create `.planning/phases/06-mobile-wui-row-actions/06-01-SUMMARY.md` with tests run, any skipped Docker-backed checks, files modified, and confirmation that no backend/TUI behavior changed.
</output>
